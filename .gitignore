node_modules/
dist/
dist-ssr/
*.local
.vite/
coverage/
.DS_Store
*.log

# TypeScript incremental build cache — regenerated by `tsc -b`, never source.
*.tsbuildinfo

# Editor / OS cruft
.idea/
Thumbs.db

# Local mock-data seeder (regenerable, dev-only)
seed.html

# Stray uploads / binaries that don't belong in source control
*.xlsx
docs/Landing/AutoClicker.exe
docs/Landing/ACLib/

# Design-import scratch — mockups/uploads pulled in during reintegration, not source
landing/files to merge/

# Local environment variables (secrets and per-machine config)
.env
.env.local
