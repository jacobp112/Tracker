/* Problem band — a static chamber with no behaviour of its own; this module
 * exists so the section's stylesheet is co-located with (and loaded by) the
 * section rather than from a central sheet. */
import './problem.css';
